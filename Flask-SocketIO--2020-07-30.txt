---------------------17:37 https://flask-socketio.readthedocs.io/en/latest/#deployment ------------------------------------------------
Receiving Messages
	- @socketio.on('message')
	- The above example uses string messages
	- @socketio.on('json')
	- JSON data:
	- @socketio.on('my event')
	- Message data for these events can be string, bytes, int, or JSON
	- Custom named events can also support multiple arguments:
	- @socketio.on('my event', namespace='/test')
	- For cases when a decorator syntax isn’t convenient, the on_event method can be used:
		• Socketio.on_event('my event', my_function_handler, namespace='/test')
Sending Messages
	- Bounce received events back to the client that sent them:
		• @socketio.on('my event') def handle_my_custom_event(json):     emit('my response', json)
	- To send an event with multiple arguments, send a tuple:
Broadcasting
	- When a message is sent with the broadcast option enabled, all clients connected to the namespace receive it, including the sender
Rooms
Authentication

