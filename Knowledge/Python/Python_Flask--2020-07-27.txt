---------------------12:25 https://code.visualstudio.com/docs/python/tutorial-flask ------------------------------------------------
Flask Tutorial in Visual Studio Code
	- Although it's possible to generate HTML directly in code, developers avoid such a practice because it opens the app to cross-site scripting (XSS) attacks
	Use a template to render a page
		- Render_template assumes that the first argument is relative to the templates folder
	Serve static files#
		- Static files are of two types
			• First
			• Stylesheets
		- Second type
			• Implement an API endpoint that returns a static file.
		- Creates a reference to the stylesheet
			• <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='site.css')}}" />
	
	If you are surprised that you are not seeing a call to the jsonify() function from Flask,
	you may not be aware that in recent releases of Flask your view function can return a dictionary, which gets 
	automatically JSONified by Flask.

	The React project can be configured to redirect any requests it receives on its port 3000 that
	it does not understand into another server. This is configured simply by adding a proxy key at the bottom package.json: